server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
    
    # API calls are handled by the external API subdomain
    # No backend proxy needed in frontend containers
}
